meta {
  name: tfm_tenis
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Colección de llamadas al backend para el TFM - Plataforma web para gestión de competiciones de tenis
}
